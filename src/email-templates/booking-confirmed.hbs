<html>
  <head>
    <meta charset="UTF-8" />
    <title>{{subject}}</title>
    <style>
      body { font-family: Arial, sans-serif; color: #333; background-color:
      #f9f9f9; padding: 20px; } .container { background: #fff; max-width: 600px;
      margin: 0 auto; border-radius: 8px; padding: 30px; box-shadow: 0 2px 6px
      rgba(0,0,0,0.1); } .status { background: #4caf50; color: #fff; padding:
      8px 12px; border-radius: 4px; display: inline-block; margin-bottom: 20px;
      font-weight: bold; } .footer { margin-top: 30px; font-size: 12px; color:
      #888; text-align: center; } .guest { margin-bottom: 15px; } .guest h4 {
      margin: 0 0 5px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="status">Booking Confirmed</div>

      <h2>Hello {{name}},</h2>
      <p>
        Thank you for choosing
        {{companyName}}. Your booking request has been confirmed.
      </p>

      <p><strong>Booking Details:</strong></p>
      <p>Booking Date: {{date}}</p>

      {{! Loop over guests }}
      {{#each guests}}
        <div class="guest">
          <h4>Guest {{addOne @index}}: {{this.name}}</h4>
          <ul>
            {{#each this.programs}}
              <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
      {{/each}}

      <p>
        If you have any questions or need to make changes, please reply to this
        email.
      </p>

      <div class="footer">
        ©
        {{year}}
        {{companyName}}. All rights reserved.
      </div>
    </div>
  </body>
</html>